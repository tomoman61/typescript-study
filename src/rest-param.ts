export { };

// const array1 = [1, 2, 3, 4];
// const reducer = (accumulator: number, currentValue: number): number => {
//   return accumulator + currentValue;
// };

// const sum = (...values: number[]): number => {
//   const numAry: number[] = values;
//   const sum = numAry.reduce(reducer);
//   console.log(sum);
//   return sum;
// };

// sum(1, 2, 3, 4, 5);